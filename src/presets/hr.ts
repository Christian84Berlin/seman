import { Schema } from '../schema';

/**
 * Standard Anonymization Rules for HR Data (Placeholder)
 * Planned for Phase 1
 */
export const HRPreset = {
  // Example placeholder
  salaryBand: Schema.number(), // Will map 65000 -> "Band 3 (Senior)"
};

