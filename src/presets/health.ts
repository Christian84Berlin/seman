import { Schema } from '../schema';

/**
 * Standard Anonymization Rules for Health Data (Placeholder)
 * Planned for Phase 1
 */
export const HealthPreset = {
  // Example placeholder
  icd10Category: Schema.string(), // Will map F32.9 -> "Affektive Störung"
};

